import React from "react";
import "./Header.css";

const Header = props => (
<header className="header">
    <h1>Clicky Game!</h1>
    <h2>
        Click on an image to begin and earn points, but do not click on them more than once!
    </h2>
</header>
);

export default Header;